<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Available Courses</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2 class="mb-0">Available Courses</h2>
      <a href="/" class="btn btn-outline-secondary">Home</a>
    </div>

    {{#courses}}
    <div class="card mb-3 shadow-sm">
      <div class="card-body">
        <h4 class="card-title">{{name}}</h4>
        <p class="card-text">
          <strong>Duration:</strong> {{duration}}<br>
          <strong>Description:</strong> {{description}}<br>
          <strong>Date:</strong> {{date}}<br>
          <strong>Time:</strong> {{time}}<br>
          <strong>Location:</strong> {{location}}<br>
        </p>
        <a href="/course/{{_id}}" class="btn btn-primary">View Details</a>
      </div>
    </div>
    {{/courses}}

    {{^courses}}
      <p class="text-muted">No courses available at the moment. Please check back soon.</p>
    {{/courses}}

    <h3 class="mt-5">Upcoming Courses</h3>

    {{#upcomingCourses}}
      <div class="card mb-3 shadow-sm">
        <div class="card-body">
          <h4 class="card-title">{{name}}</h4>
          <p class="card-text">
            {{#date}}<strong>Date:</strong> {{date}} at {{time}}<br>{{/date}}
            {{#location}}<strong>Location:</strong> {{location}}<br>{{/location}}
            <strong>Duration:</strong> {{duration}}<br>
            {{#description}}<strong>Description:</strong> {{description}}<br>{{/description}}
          <a href="/course/{{_id}}" class="btn btn-primary">View Details</a>
        </div>
      </div>
    {{/upcomingCourses}}
  </div>
</body>
</html>